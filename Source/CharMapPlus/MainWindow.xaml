<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="CharMapPlus.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CharMapPlus"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:CharMapPlus.ViewModels"
    xmlns:cv="using:CharMapPlus.Converters"
    xmlns:ctl="using:CharMapPlus.Views.Controls"
    mc:Ignorable="d"
    Title="Character map">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid x:Name="RootGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="WindowStates">
                <VisualState x:Name="NarrowState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="FontsComboBox.Visibility" Value="Visible" />
                        <Setter Target="FontsListView.Visibility" Value="Collapsed" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="WideState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="800" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="FontsComboBox.Visibility" Value="Collapsed" />
                        <Setter Target="FontsListView.Visibility" Value="Visible" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <!--<ComboBox x:Name="FontsComboBox"
                  Grid.Column="1"
                  MaxWidth="400"
                  Margin="12,6"
                  ItemsSource="{x:Bind ViewModel.Fonts, Mode=OneWay}"
                  SelectedItem="{x:Bind ViewModel.SelectedFont, Mode=TwoWay}"
                  Visibility="Collapsed"
                  HorizontalAlignment="Stretch"
                  VerticalAlignment="Center" />-->

        <ctl:FontList x:Name="FontListControl"
                      Grid.RowSpan="3"
                      Margin="0" />

        <ctl:GlyphExplorer x:Name="GlyphExplorerControl"
                          Grid.Column="1"
                          Grid.Row="1"
                          Margin="0" />

        <Border Padding="15"
                Grid.Column="1"
                Grid.Row="3">
            <Grid x:Name="SelectionGrid"
                  MaxWidth="640"
                  VerticalAlignment="Center"
                  ColumnSpacing="5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Text="Caracteres para copiar:" VerticalAlignment="Center" />
                <TextBox Grid.Column="1" Text="{x:Bind ViewModel.SelectionText, Mode=TwoWay}" />
                <Button Grid.Column="2" Command="{x:Bind ViewModel.CopySelectionCommand}">Seleccionar</Button>
                <Button Grid.Column="3" Command="{x:Bind ViewModel.CopySelectedTextCommand}">Copiar</Button>
            </Grid>
        </Border>
    </Grid>
</Window>
